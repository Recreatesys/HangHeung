<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-inherit="point_of_sale.ProductCard" t-inherit-mode="extension">

        <xpath expr="//div[contains(@class, 'product-content')]" position="replace">
            <div class="product-content d-flex flex-column px-2 rounded-bottom rounded-3 flex-shrink-1" 
                 t-att-class="{'h-100' : !props.imageUrl}">

                <div class="overflow-hidden lh-sm product-name my-1 fs-4 text-wrap text-break"
                    t-att-class="{'no-image d-flex justify-content-center align-items-center text-center fs-4': !props.imageUrl}"
                    t-attf-id="article_product_{{props.productId}}"
                    t-esc="props.name" />

                <span t-if="props.productCartQty"
                      t-out="this.productQty"
                      class="product-cart-qty fw-bolder fs-2 mt-1 text-end" />
            </div>
        </xpath>

    </t>
</templates>
